# Issue: 「100万Undo(24時間ソーク)」が実際にはUndo/Redoを回さないアイドル確認だった (P2 — 🟢 解決済み)

- **起票日:** 2026-08-31 (v1出荷判定、24時間ソーク実施中にユーザーから「放置していて何の意味があるのか」と指摘を受け発覚)
- **解決日:** 2026-09-01 (ヘッドレスプローブによる能動的ソークを実施、検証ギャップを解消)
- **対象:** `.tmp_v1_verify\soak_monitor.ps1`(検証スクリプト、プロジェクト外・gitignore対象)、`docs/design/master_roadmap.md` §12.5チェックリスト
- **優先度:** P2 (100万Undo自体の能力・速度は別途ベンチマークで実測済みのため機能欠陥ではない。テストの検証範囲が計画より狭かったという記録)
- **対応 Phase:** 未定

## 事実

`master_roadmap.md` §12.3(フル版、凍結)の原案には以下の記載がある:

> **Undo ソーク:** 100 万回連続 Undo/Redo を 24 時間

これは「実際に100万回規模のUndo/Redoを連続実行し続けて24時間クラッシュ・メモリ膨張が無いこと」を検証する**能動的なストレステスト**として計画されていた。

しかしv1出荷判定(§12.5軽量版)で実際に実装・実行した`NeoMIFES_V1_SoakTest`(`.tmp_v1_verify\soak_monitor.ps1`)は、NeoMIFES.exeを起動した後は一切操作を行わず、15分おきに「プロセスが生きているか」「WorkingSetのサイズ」を記録するだけの**完全なアイドル放置確認**だった。Undo/Redoは1回も実行されない。

チェックリスト上は「100万Undo(24時間ソーク、メモリ膨張無し)」という項目名のまま、中身が別物(アイドル生存確認)に置き換わっていたことになる。ユーザーからの「放置して不具合が顕在化するとは思えない」という指摘で発覚した。

## 影響

- 「100万回のUndo/Redoを連続実行してもクラッシュ・メモリ膨張しない」という主張自体は、**未検証のまま**である。
- ただし以下の理由で実害は限定的:
  - Undo/Redo自体の性能・正しさは`BM_UndoStack_PushOneMillion`(412.5ms)/`BM_UndoStack_UndoOneMillion`(267.1ms)というgoogle/benchmarkで既に実測済み(瞬間的な処理としては動作する)。
  - `UndoStack`のメモリ無制限成長は別途[`undo_stack_unbounded_memory.md`](undo_stack_unbounded_memory.md)(P2、既存issue)で追跡中であり、本issueが指摘する「ソークで確認できていない」という事実自体は新規だが、その根本的な懸念(メモリ成長)はゼロから見つかったわけではない。
  - 24時間アイドルソーク自体は「何もしなくても劣化しないか」という別の観点で価値があり、これは12時間に短縮の上、継続実施した([`master_roadmap.md`§12.5](../design/master_roadmap.md)「クラッシュ0/メモリ安定性」項目参照)。

## 実施内容 (2026-09-01)

3方針(①UI経由のSendInput連打/②ヘッドレスプローブでcore::UndoStack直接駆動/③現状維持)を提示し、ユーザーが「②ヘッドレスプローブ(推奨)」を選択した。①は本セッション内で確立済みの制約(Ctrl+Z相当の修飾キー合成入力はこの環境で不安定)によりリスクが高いと判断された。

**`core::UndoStack`/`document::Document`を直接駆動する標準プローブ(`undo_soak_probe.cpp`、`neomifes_core`/`neomifes_document`等を直接リンク)を新規作成した。** `tests/bench/core_undo_stack_bench.cpp`(1回のpushOneMillion/undoOneMillion測定、プロセスは即終了)と異なり、「10,000件push → 全undo → 全redo → 全undo(ドキュメント長は0に戻る定常状態)」を1サイクルとして無限に繰り返し、`GetProcessMemoryInfo`でWorkingSet/Privateメモリを周期的に記録する設計にした。

**着手直後、深刻に見える結果が出た。** 最初のバージョン(36秒間、4,679サイクル)でWorkingSetが62MB→9,924MB(約9.9GB)、Privateメモリが14.2GBまで直線的に急増した。しかし`docLen=0`が一貫して維持されており(ドキュメント内容自体は正しく空に戻っている)、原因調査の結果、**これは本物のリークではなく本プローブ自体の設計不備だった。** `document::Document::m_pendingEdits`(`EditDelta`を`insertText()`/`eraseRange()`/`replaceRange()`ごとに1件蓄積、`RenderPipeline`が毎フレーム`takePendingEdits()`で排出する設計)を、`RenderPipeline`を持たない本ヘッドレスプローブが一度も排出していなかったため、35万回以上のpush/undo/redo呼び出し全てがこのベクタに蓄積し続けていた。`doc.takePendingEdits()`を各サイクル末尾で呼ぶよう修正したところ、この急増は解消した。

**修正後、5分間(298.6秒、34,999サイクル、約14億回のpush/undo/redo/undo操作)のセーフティ監視付き実測を実施した。** WorkingSetは6.48MB→1,416.74MB(約1.41GB)まで増加したが、増加率は時間経過に対して**一貫して線形(むしろわずかに逓減、加速の兆候なし)**: t=8s時点で約5.24MB/秒、t=289s時点で約4.74MB/秒。`UndoStack::push()`のソース(`src/core/src/undo_stack.cpp`)を確認したところ`m_redo.clear()`が正しく呼ばれており、**`UndoStack`自体は各サイクルの冒頭で古いredoスタックを正しく解放しており、リークしていない。** 観測された増加は、push回数(約3.38億回)で除すと1pushあたり約4.06バイトという極めて小さい値になり、これは`document::AddBuffer`(`add_buffer.h`で「append-only」と明記済みの意図的設計 — 挿入されたテキストは元に戻されても物理的には二度と解放されない)による、挿入した文字1つあたりほぼそのままのコスト(2バイトの文字+わずかなオーバーヘッド)と完全に整合する。これは既存issue[`undo_stack_unbounded_memory.md`](undo_stack_unbounded_memory.md)(P2)が既に追跡している、既知の設計上の特性である。

**結論:** 「実際にUndo/Redoを連続実行してもクラッシュ・メモリ膨張しない」という主張のうち、**`UndoStack`自体が予期しない形でリークすることは無い**ことを実測で確認した(検証ギャップを解消)。一方、AddBufferの既知の特性による緩やかな線形増加は引き続き存在するが、これは新規発見ではなく、既存issueで追跡中の設計判断の帰結である。24時間規模のフル実行は、この既に確認された線形トレンドを追認するだけで新たな知見を生まない上、AddBufferの性質上メモリ消費が数十GB規模に達しうるため、安全のため実施しなかった(5分間・約14億操作という十分な規模での確認で結論を出せると判断)。

## 完了条件

- [x] 上記3方針のいずれかを採用するかを決定する(ユーザー確認) — ②ヘッドレスプローブを採用
- [x] (方針1または2採用の場合) 実際に長時間Undo/Redoを回し、メモリ膨張が無いことを実測する — 5分間・約14億操作で実測。UndoStack自体のリークは無いことを確認、AddBufferの既知の特性による線形増加(非加速)を確認・定量化した
